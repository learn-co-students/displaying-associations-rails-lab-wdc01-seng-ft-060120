<h1>All Artists </h1>

<div>
    <ul>
        <% @artists.each do |artist| %>
            <li><%= link_to "#{artist.name} - #{artist.song_count} songs", artist_path(artist.id) %></li>
        <% end %>
    </ul>
</div>

<%= link_to "Add an Artist", new_artist_path %>